spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    defer-datasource-initialization: true
    database-platform: org.hibernate.dialect.MariaDBDialect
  datasource:
    url: jdbc:mariadb://db:3306/db25109
    driver-class-name: org.mariadb.jdbc.Driver
    username: dbid251
    password: dbpass251

  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

  sql:
    init:
      mode: always

jwt:
  issuer: yjj1544@gmail.com
  secretKey: maum-record

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    enabled: true

server:
  port: 8080